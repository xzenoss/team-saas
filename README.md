# Gather

A calm team workspace for projects, tasks, and shared progress.

## Run locally

```powershell
python server.py --port 8000
```

Open http://127.0.0.1:8000.

The app uses SQLite and creates its local database under `data/`. Runtime data and secrets are excluded from Git.
